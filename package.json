{
  "private": true,
  "scripts": {
    "build:css": "node-sass -o css scss",
    "build:js": "tsc --build src/tsconfig.json",
    "build": "run-p build:*"
  },
  "devDependencies": {
    "aws-sign2": "^0.7.0",
    "node-sass": "^4.14.1",
    "npm-run-all": "^4.1.5",
    "typescript": "^4.0.2"
  }
}
